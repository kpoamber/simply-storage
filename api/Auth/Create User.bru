meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "username": "newuser",
    "password": "password123",
    "role": "user"
  }
}
