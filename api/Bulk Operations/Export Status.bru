meta {
  name: Export Status
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/storages/{{storageId}}/export/status?job_id={{exportJobId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
